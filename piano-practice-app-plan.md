# ピアノ練習アプリ 開発計画書

Claude Code への実装指示書。フェーズ順に実装し、各フェーズの「完了条件」を満たしてから次に進むこと。

---

## 1. プロジェクト概要

**コンセプト**: MIDIキーボードをブラウザに接続し、ユーザーが持ち込んだ MusicXML 譜面を「打鍵を待って進行する練習モード」で練習できる、完全無料のピアノ練習 Web アプリ。

**設計原則(最重要・全フェーズで遵守)**:

1. **完全クライアントサイド**: サーバー、API、アカウント、DB を一切持たない。すべてブラウザ内で完結させる。ランニングコストを恒久的にゼロ円にするための絶対条件。
2. **コンテンツを持たない**: 著作権のある楽曲は同梱・配信しない。譜面はユーザーが自分のファイルを読み込む方式。同梱するサンプル曲はパブリックドメインのみ。
3. **静的ホスティングで動く**: ビルド成果物は静的ファイルのみ。GitHub Pages / Cloudflare Pages にそのままデプロイできること。

**ターゲットユーザー**: MIDI 接続可能な電子ピアノ/キーボードを持つ独学練習者。PC(Chrome/Edge/Firefox)での利用を想定。

---

## 2. スコープ

### MVP でやること

- Web MIDI API によるキーボード接続とノート入力の取得
- MusicXML(.xml / .musicxml / .mxl)の読み込みと譜面表示
- **練習モード**: 正しい鍵盤を押すまでカーソルが進まない wait 方式(本アプリの核)
- 右手のみ / 左手のみ / 両手のパート選択
- 小節範囲を指定してのループ練習
- 画面下部のバーチャル鍵盤表示(期待ノートと実際の打鍵をハイライト)
- 練習記録の localStorage 保存(曲ごとの練習時間・回数)
- パブリックドメインのサンプル曲を数曲同梱

### MVP でやらないこと(バックログ行き)

- 採点/プレイモード(メトロノーム同期のリズム判定)— Phase 7 として計画のみ記載
- 音声(マイク)によるアコースティックピアノ対応
- MIDI ファイル(.mid)からの譜面生成
- ソフトウェア音源での再生(電子ピアノ自体が発音するため不要)
- アカウント・クラウド同期・多言語対応

---

## 3. 技術スタック

| 領域         | 選定                                 | 理由                                                       |
| ------------ | ------------------------------------ | ---------------------------------------------------------- |
| ビルド       | Vite + TypeScript                    | 静的出力、高速、型安全                                     |
| UI           | React 18                             | エコシステムと実装速度                                     |
| 譜面表示     | OpenSheetMusicDisplay (OSMD)         | MusicXML レンダリングとカーソル API を持つ事実上の標準 OSS |
| MIDI 入力    | Web MIDI API(直接使用)               | 依存を減らす。薄いラッパーを自作                           |
| スタイル     | Tailwind CSS + CSS変数               | 実装速度とテーマ一貫性                                     |
| 状態管理     | React hooks + Context(必要最小限)    | MVP に外部状態ライブラリは不要                             |
| .mxl 展開    | JSZip                                | 圧縮 MusicXML 対応                                         |
| テスト       | Vitest                               | 採点・進行判定ロジックのユニットテスト                     |
| ホスティング | GitHub Pages(Actions で自動デプロイ) | 無料・無限に維持可能                                       |

**ブラウザ対応**: Chrome / Edge / Firefox(Web MIDI 対応ブラウザ)。Safari は Web MIDI 非対応のため対象外とし、非対応ブラウザには案内画面を表示する。

---

## 4. アーキテクチャ方針

```
UI層 (React components)
  ├─ ScoreView        … OSMD ラッパー。譜面描画とカーソル制御
  ├─ VirtualKeyboard  … 88鍵表示。期待音=青、正打=緑、誤打=赤
  ├─ ControlBar       … パート選択 / 小節範囲 / リセット
  └─ FilePicker       … ドラッグ&ドロップ + ファイル選択

コアロジック層 (純粋TS、Reactに依存しない ← ユニットテスト対象)
  ├─ midi/            … Web MIDI ラッパー(デバイス列挙、NoteOn/Off購読)
  ├─ score/           … OSMDカーソルから「期待ノート集合」を順に取り出すイテレータ
  ├─ practice/        … 練習モードのステートマシン(下記 §5.3)
  └─ storage/         … localStorage の読み書き(スキーマは §8)
```

コアロジックは React から分離し、`practice/` は「現在押されている MIDI ノート集合を入力すると、進行/待機/誤打を返す純粋関数群」として実装する。これがテスト容易性の要。

---

## 5. 機能仕様(MVP)

### 5.1 MIDI 接続

- 起動時に `navigator.requestMIDIAccess()` を要求。拒否/非対応時は接続手順の案内を表示。
- 複数デバイスがある場合はセレクタで選択。デバイスの抜き差し(statechange)に追随。
- NoteOn(velocity>0)/ NoteOff を購読し、「現在押下中のノート番号の Set」を常に保持する。

### 5.2 譜面読み込みと表示

- `.xml` / `.musicxml` はテキストとして、`.mxl` は JSZip で展開して OSMD に渡す。
- 読み込み失敗時はエラー理由を日本語で表示(壊れたファイル、非対応要素など)。
- OSMD のカーソルを表示し、練習位置を明示する。
- パート選択(右手/左手/両手)は、OSMD の譜表(staff)単位で判定対象をフィルタする。表示は常に大譜表のまま、判定対象外の譜表は薄く描画できれば尚良い(困難なら判定のみ切り替えで可)。

### 5.3 練習モード(本アプリの核)

ステートマシンとして実装する:

- **WAITING**: カーソル位置の「期待ノート集合」(和音なら複数)の充足を待つ。
  - 期待ノートを押した → そのノートを「充足済み」にマーク(緑表示)。
  - 期待外のノートを押した → 誤打として赤表示 + カウント。**進行はブロックしない**(充足済みは維持)。
  - 期待ノート集合がすべて充足 → **ADVANCE** へ。
- **ADVANCE**: カーソルを次の音符位置へ進め、充足状態をリセットして WAITING に戻る。曲末なら **FINISHED**。
- **FINISHED**: 所要時間・誤打数を表示し、リプレイを促す。localStorage に記録を保存。

**期待ノート集合の抽出ルール**:

- 同一カーソル位置の全 Voice・全対象譜表の音符を集合にする(和音対応)。
- 休符のみの位置はスキップして次の音符位置へ自動前進。
- **タイで繋がれた後続音は期待集合から除外**(押し直し不要)。
- 装飾音(grace note)は MVP では判定対象外としてスキップ。
- 同音連打は NoteOff → NoteOn の押し直しを要求する。

**ループ練習**: 開始/終了小節を指定すると、その範囲で FINISHED 後に自動で先頭へ戻る。

### 5.4 バーチャル鍵盤

- 88鍵を画面下部に常時表示。
- 期待ノート=青、充足(正打)=緑、誤打=赤、単なる押下=グレーでリアルタイム表示。
- 音名表示(C4 など)のトグル。

### 5.5 練習記録

- 曲(ファイル名+内容ハッシュで同定)ごとに、練習回数・累計時間・最終練習日・ループ完走数を localStorage に保存。
- ホーム画面に「最近練習した曲」リストを表示(ファイル自体は保存しない。再読み込みはユーザー操作。File System Access API での再オープン補助はバックログ)。

### 5.6 サンプル曲

- パブリックドメイン曲の MusicXML を 3〜5 曲同梱(例: バイエルやブルクミュラーの平易な曲、メヌエット ト長調など)。出典とパブリックドメインである根拠を README に明記すること。信頼できる PD ソース(Mutopia Project / OpenScore 等の PD・CC0 素材)から取得するか、自前で打ち込む。

---

## 6. フェーズ計画

各フェーズは独立して動作確認できる単位。**完了条件を満たしてから次へ**。

### Phase 0: セットアップ

Vite + React + TS + Tailwind + Vitest の雛形、ESLint/Prettier、GitHub Actions で Pages デプロイまで通す。

- ✅ 完了条件: 空アプリが GitHub Pages で公開される。`npm test` が動く。

### Phase 1: MIDI 入力

`midi/` モジュールと接続 UI。押下中ノート集合の管理。

- ✅ 完了条件: 実機(または仮想 MIDI)の打鍵が画面のログとバーチャル鍵盤に即時反映される。デバイス抜き差しに追随する。

### Phase 2: 譜面読み込み・表示

FilePicker、.xml/.mxl 対応、OSMD 描画、カーソル表示、サンプル曲同梱。

- ✅ 完了条件: サンプル曲と手持ちの MusicXML が表示され、カーソルを手動ボタンで音符単位に進められる。

### Phase 3: 期待ノート抽出

`score/` モジュール。カーソル位置→期待ノート集合(MIDI番号の Set)の変換。タイ・休符・和音の処理。

- ✅ 完了条件: 代表パターン(単音/和音/タイ/休符/両手同時)のユニットテストが通る。

### Phase 4: 練習モード結合

`practice/` ステートマシンを実装し、MIDI 入力と譜面を結合。誤打表示、完走画面。

- ✅ 完了条件: サンプル曲を実機で最初から最後まで「弾いて進める」ことができる。誤打しても正しく待機し続ける。

### Phase 5: 練習 UX

パート選択、小節範囲ループ、鍵盤ハイライトの仕上げ、非対応ブラウザ案内。

- ✅ 完了条件: 右手のみで 4〜8 小節をループ練習する一連の流れが破綻なく行える。

### Phase 6: 記録と仕上げ

localStorage 記録、ホーム画面、エラーハンドリング総点検、README(使い方・PD 曲の出典)。

- ✅ 完了条件: リロード後も練習記録が残る。初見のユーザーが README なしで練習開始まで辿り着ける導線がある。

### Phase 7(バックログ・MVP後): 採点モード

メトロノーム同期で曲が自動進行し、タイミング×音程で採点。設計だけ将来検討とし、MVP では着手しない。

---

## 7. 技術的難所と対処方針

1. **OSMD カーソルからの音符取得**: `cursor.NotesUnderCursor()` / `cursor.Iterator` を使う。OSMD のバージョンにより API 差異があるため、Phase 3 の最初に現行版の API を確認し、`score/` 内に隔離すること(OSMD 依存を UI やステートマシンに漏らさない)。
2. **ピッチ→MIDI 番号変換**: OSMD の Pitch(halfTone 値)から変換する。異名同音(C#=Db)は MIDI 番号で比較するため問題にならない。
3. **和音の「同時」判定**: 厳密な同時押しは要求しない。充足済みマークを保持する方式(§5.3)により、順番にゆっくり押しても進行できる。これは初心者練習として意図した仕様。
4. **反復記号・D.C./D.S.**: OSMD カーソルの標準挙動(リピート展開)に従う。異常があれば「リピート無視オプション」で逃げる。
5. **巨大な譜面のパフォーマンス**: MVP ではページ全体レンダリングで進め、重い場合のみ OSMD のオプション(描画範囲制限)を検討。早すぎる最適化をしない。

---

## 8. データ設計(localStorage)

```ts
// key: "ppa:v1:records"
type Records = {
  [scoreId: string]: {
    // scoreId = fileName + ":" + sha256(content).slice(0,16)
    title: string // MusicXML の movement-title か fileName
    playCount: number
    totalPracticeSec: number
    lastPlayedAt: string // ISO 8601
    completions: number // 完走(ループ含む)回数
  }
}

// key: "ppa:v1:settings"
type Settings = {
  midiDeviceId?: string
  showNoteNames: boolean
}
```

スキーマ変更時はキーの `v1` を上げてマイグレーション関数を書く。

---

## 9. ディレクトリ構成

```
src/
  core/                 # React 非依存の純粋ロジック(テスト対象)
    midi/
    score/
    practice/
    storage/
  components/
    ScoreView.tsx
    VirtualKeyboard.tsx
    ControlBar.tsx
    FilePicker.tsx
    Home.tsx
  App.tsx
  main.tsx
public/
  samples/              # パブリックドメイン MusicXML
tests/                  # core/ のユニットテスト
```

---

## 10. UI / デザイン方針

- 方向性: **「紙の楽譜と練習室」を思わせる、静かで上質なミニマル**。譜面が主役なので UI は徹底的に引く。生成 AI にありがちな紫グラデーションや Inter フォントの汎用スタイルは禁止。
- 配色: 温かみのある紙白(例 #FAF7F2)背景 + 墨色の文字 + アクセント 1 色(深い青緑系)。ダークモードは不要。
- タイポグラフィ: 見出しにセリフ体(和文は明朝系)、UI 部は可読性の高いサンセリフ。CSS 変数でトークン化。
- 練習中の情報表示(誤打・進捗)は視界の邪魔をしない控えめなトースト/バッジで。
- レイアウト: 上=譜面(可変高)、下=88鍵、右肩に最小限のコントロール。練習中は譜面と鍵盤以外を極力消す。

---

## 11. テスト方針

- `core/` はユニットテスト必須。特に **期待ノート抽出(タイ/和音/休符)** と **練習ステートマシン(誤打・充足保持・完走)** はテーブル駆動で網羅する。
- MIDI はインターフェースを切ってモック注入(実機依存のテストを CI に持ち込まない)。
- UI は MVP では手動確認で可。E2E はバックログ。

---

## 12. バックログ(MVP 後の拡張候補)

優先度順:

1. 採点/プレイモード(Phase 7)
2. 初見練習の譜面自動生成(著作権フリーの無限コンテンツ源)
3. テンポ指定つき自動再生(モデル演奏)とソフト音源
4. File System Access API による最近ファイルの再オープン
5. PWA 化(オフライン練習)
6. スケール・アルペジオ練習モード
7. OSS 公開(MIT ライセンス)+ GitHub Sponsors 設置
8. 英語 UI(i18n)

---

## 13. Claude Code への進行指示

- フェーズごとにブランチを切り、完了条件を満たしたら main へマージする。
- 各フェーズ完了時に、動作確認手順(何をどう触れば確認できるか)をコミットメッセージか PR 説明に書く。
- 実機 MIDI がない環境では、開発用に「画面クリックで NoteOn/Off を発火するデバッグ鍵盤」を最初に作り、それで代替確認する。
- 不明点・仕様の穴を見つけた場合は、この計画書の該当節を更新してから実装する(計画書を常に真実のソースに保つ)。
